# Copilot instructions

Follow `AGENTS.md` as the primary repository guide.

In particular, use test-driven development by default:

1. write a failing test first
2. make the smallest change to pass it
3. refactor only after tests are green

Do not introduce behavior changes without automated test coverage for that behavior.
